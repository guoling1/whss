<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.admin.dao.RevokeQrCodeRecordDao">
    <sql id="Base_Column_List">
        id,start_code,end_code,success_count,fail_count,sys_type,operator_id,platform_type,status,create_time,update_time
    </sql>
    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_revoke_qr_code_record
        (
        start_code,
        end_code,
        success_count,
        fail_count,
        sys_type,
        operator_id,
        platform_type,
        status
        )
        VALUES
        (
        #{startCode},
        #{endCode},
        #{successCount},
        #{failCount},
        #{sysType},
        #{operatorId},
        #{platformType},
        #{status}
        );
    </insert>
</mapper>
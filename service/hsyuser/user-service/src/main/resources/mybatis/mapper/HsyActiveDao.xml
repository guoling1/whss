<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hsy.user.dao.HsyActiveDao">
    <select id="findLoginInfoByAccessToken" resultType="com.jkm.hsy.user.entity.AppAuUserToken">
        select
          ut.UID,ut.TID,ut.STATUS,ut.LOGINTIME,ut.OUTTIME
        from app_au_token token left join app_au_user_token ut on token.ID=ut.TID
        where token.ACCESSTOKEN=#{accessToken} and ut.status=1
    </select>
</mapper>
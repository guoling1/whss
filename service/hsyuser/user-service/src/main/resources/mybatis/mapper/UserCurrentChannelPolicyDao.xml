<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hsy.user.dao.UserCurrentChannelPolicyDao">

    <sql id="Base_Column_List">
        id,	user_id,wechat_channel_type_sign,alipay_channel_type_sign,status,create_time,update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user_current_channel_policy(user_id,wechat_channel_type_sign,alipay_channel_type_sign,status)
        VALUES(#{userId},#{wechatChannelTypeSign},#{alipayChannelTypeSign},#{status})
    </insert>

    <select id="selectByUserId" resultType="com.jkm.hsy.user.entity.UserCurrentChannelPolicy">
        SELECT <include refid="Base_Column_List"/> FROM app_user_current_channel_policy
        WHERE user_id=#{userId} and status = 1;
    </select>

    <update id="updateByUserId">
        UPDATE app_user_current_channel_policy
        SET
        wechat_channel_type_sign = #{wechatChannelTypeSign},
        alipay_channel_type_sign = #{alipayChannelTypeSign}
        WHERE user_id = #{userId}
    </update>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hsy.user.dao.AdvertisementDao">
    <select id="getByTime" resultType="com.jkm.hsy.user.entity.Advertisement">
        SELECT `id`,
    `titleName`,
    `content`,
    `validStartDate`,
    `validEenDate`,
    `picUrl`,
    `jumpUrl`,
    `status`,
    `duration`,
    `remark`,
    `create_time`,
    `update_time`
    FROM `tb_advertisement`
    WHERE validStartDate <![CDATA[ <= ]]> #{time}
        and validEenDate <![CDATA[ >= ]]> #{time}
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.notifier.dao.ConsumeMsgSplitProfitRecordDao">
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into tb_consume_msg_split_profit_record
        (

        request_param,
        tag,
        status
        )
        values
        (
        #{requestParam},
        #{tag},
        #{status}
        )
    </insert>

    <update id="updateStatus">
        UPDATE tb_consume_msg_split_profit_record SET status = #{status} WHERE id = #{id}
    </update>

    <select id="selectById" resultType="com.jkm.hss.notifier.entity.ConsumeMsgSplitProfitRecord">
        SELECT * FROM tb_consume_msg_split_profit_record WHERE id = #{id}
    </select>

    <select id="selectByIdWithLock" resultType="com.jkm.hss.notifier.entity.ConsumeMsgSplitProfitRecord">
        SELECT * FROM tb_consume_msg_split_profit_record WHERE id = #{id} FOR UPDATE
    </select>

    <select id="selectPendingRecordsByTag" resultType="com.jkm.hss.notifier.entity.ConsumeMsgSplitProfitRecord">
        SELECT * FROM tb_consume_msg_split_profit_record
        WHERE tag = #{tag}
        AND status = 1
        LIMIT 100
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.notifier.dao.ConsumeMsgFailRecordDao">
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into tb_consume_msg_fail_record
        (
        request_param,
        tag,
        status
        )
        values
        (
        #{requestParam},
        #{tag},
        #{status}
        )
    </insert>

    <update id="updateStatus">
        UPDATE tb_consume_msg_fail_record SET status = #{status} WHERE id = #{id}
    </update>

    <select id="selectPendingRecordsByTag" resultType="com.jkm.hss.notifier.entity.ConsumeMsgFailRecord">
        SELECT * FROM tb_consume_msg_fail_record
        WHERE tag = #{tag}
        AND status = 1
        LIMIT 100
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hsy.user.dao.AppQrcodeShopRelDao">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id,	code, shop_id,	status,	create_time,	update_time
    </sql>

    <!-- 添加 （匹配有值的字段）-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_qrcode_shop_rel
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="code != null">
                code,
            </if>
            <if test="shopId != null">
                shop_id,
            </if>
            <if test="status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="code!=null">
                #{code},
            </if>
            <if test="shopId != null">
                #{shopId},
            </if>
            <if test="status!=null">
                #{status},
            </if>
        </trim>
    </insert>

    <!-- 查询（根据req_sn查询） -->
    <select id="selectByCode" resultType="com.jkm.hss.product.entity.UpgradePayRecord">
        SELECT
        <include refid="Base_Column_List" />
        FROM app_qrcode_shop_rel
        WHERE code = #{code} and status=1
    </select>


</mapper>
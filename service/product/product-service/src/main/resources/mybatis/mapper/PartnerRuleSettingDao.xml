<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.product.dao.PartnerRuleSettingDao">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id,	product_id,  channel_type_sign,	default_profit_space,	common_rate,	clerk_rate,	shopowner_rate,	boss_rate,	status,	create_time,	update_time
    </sql>

    <!-- 添加 （匹配有值的字段）-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_partner_rule_setting
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="productId != null">
                product_id,
            </if>
            <if test="channelTypeSign != null">
                channel_type_sign,
            </if>
            <if test="defaultProfitSpace != null">
                default_profit_space,
            </if>
            <if test="commonRate != null">
                common_rate,
            </if>
            <if test="clerkRate != null">
                clerk_rate,
            </if>
            <if test="shopownerRate != null">
                shopowner_rate,
            </if>
            <if test="bossRate != null">
                boss_rate,
            </if>
            <if test="status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="productId!=null">
                #{productId},
            </if>
            <if test="channelTypeSign!=null">
                #{channelTypeSign},
            </if>
            <if test="defaultProfitSpace!=null">
                #{defaultProfitSpace},
            </if>
            <if test="commonRate!=null">
                #{commonRate},
            </if>
            <if test="clerkRate!=null">
                #{clerkRate},
            </if>
            <if test="shopownerRate!=null">
                #{shopownerRate},
            </if>
            <if test="bossRate!=null">
                #{bossRate},
            </if>
            <if test="status!=null">
                #{status},
            </if>
        </trim>
    </insert>

    <!-- 修 改-->
    <update id="update">
        UPDATE tb_partner_rule_setting
        <set>
            <if test="productId != null">
                product_id = #{productId},
            </if>
            <if test="channelTypeSign != null">
                channel_type_sign = #{channelTypeSign},
            </if>
            <if test="defaultProfitSpace != null">
                default_profit_space = #{defaultProfitSpace},
            </if>
            <if test="commonRate != null">
                common_rate = #{commonRate},
            </if>
            <if test="clerkRate != null">
                clerk_rate = #{clerkRate},
            </if>
            <if test="shopownerRate!=null">
                shopowner_rate = #{shopownerRate},
            </if>
            <if test="bossRate!=null">
                boss_rate = #{bossRate},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectAllByProductId" resultType="com.jkm.hss.product.helper.response.PartnerRuleSettingResponse">
        select p.id,p.product_id, p.channel_type_sign,p.default_profit_space,p.common_rate,	p.clerk_rate,p.shopowner_rate,p.boss_rate,b.channel_short_name as channel_name
        from tb_partner_rule_setting as p left join tb_basic_channel as b on p.channel_type_sign=b.channel_type_sign
        where p.product_id =#{productId} and p.status=1 order by p.create_time desc
    </select>

    <select id="selectById" resultType="com.jkm.hss.product.entity.PartnerRuleSetting">
        select <include refid="Base_Column_List" /> from tb_partner_rule_setting where id =#{id} and status=1
    </select>
    <select id="selectByProductIdAndChannelSign" resultType="com.jkm.hss.product.entity.PartnerRuleSetting">
        SELECT <include refid="Base_Column_List"/> FROM tb_partner_rule_setting WHERE product_id =#{productId} AND channel_type_sign = #{channelSign} and status=1
    </select>

    <select id="selectByProductIdAndChannelTypeSign" resultType="com.jkm.hss.product.entity.PartnerRuleSetting">
        select <include refid="Base_Column_List" /> from tb_partner_rule_setting where product_id =#{productId} and channel_type_sign = #{channelTypeSign} and status=1
    </select>
</mapper>
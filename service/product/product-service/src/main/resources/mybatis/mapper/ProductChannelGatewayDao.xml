<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.product.dao.ProductChannelGatewayDao">
    <insert id="addNew" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_product_channel_gateway
        (gateway_type,
        dealer_gateway_id,
        product_id,
        product_type,
        channel_sign,
        view_channel_name,
        channel_short_name,
        dealer_id,
        status
        )VALUES(
         #{gatewayType},
         #{dealerGatewayId},
         #{productId},
         #{productType},
         #{channelSign},
         #{viewChannelName},
         #{channelShortName},
         #{dealerId},
         #{status}
        )
    </insert>
    <update id="update">
        UPDATE tb_product_channel_gateway
        SET
        gateway_type = #{gatewayType},
        dealer_gateway_id = #{dealerGatewayId},
        product_id = #{productId},
        product_type = #{productType},
        channel_sign = #{channelSign},
        view_channel_name = #{viewChannelName},
        channel_short_name = #{channelShortName},
        dealer_id = #{dealerId},
        status = #{status}
        WHERE id = #{id}
    </update>
    <update id="deleteGateway">
        UPDATE tb_product_channel_gateway
        SET status = #{status}
        WHERE id = #{id}
    </update>
    <select id="selectByProductTypeAndGatewayAndProductIdAndDealerId"
            resultType="com.jkm.hss.product.entity.ProductChannelGateway">
        SELECT * FROM tb_product_channel_gateway
        WHERE product_type = #{type} AND gateway_type = #{gatewayType} AND product_id = #{productId} AND status = 2 AND gateway_type = 'product' AND dealer_id = #{dealerId}
    </select>

    <update id="recommend">
        UPDATE tb_product_channel_gateway
        SET recommend = #{recommend}
        WHERE id = #{id}
    </update>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jkm.hss.merchant.dao.ChangeMerchantNameDao">
	<update id="updatChangeName">
		UPDATE tb_merchant_info
		set merchant_change_name = #{merchantChangeName} WHERE id = #{id}
	</update>

	<select id="selectChangeName" resultType="com.jkm.hss.merchant.entity.MerchantInfo">
		SELECT merchant_change_name from tb_merchant_info where id=#{id}
	</select>
</mapper>
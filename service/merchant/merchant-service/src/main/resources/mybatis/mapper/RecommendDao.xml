<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.merchant.dao.RecommendDao">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
		 id,	merchant_id,	recommend_merchant_id,	type,status,	create_time,	update_time
	</sql>

    <!-- 添加 （匹配有值的字段）-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_recommend
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="merchantId != null">
                merchant_id,
            </if>
            <if test="recommendMerchantId != null">
                recommend_merchant_id,
            </if>
            <if test="type != null">
                type,
            </if>
            <if test="status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="merchantId!=null">
                #{merchantId},
            </if>
            <if test="recommendMerchantId!=null">
                #{recommendMerchantId},
            </if>
            <if test="type!=null">
                #{type},
            </if>
            <if test="status!=null">
                #{status},
            </if>
        </trim>
    </insert>

    <!-- 修 改-->
    <update id="update">
        UPDATE tb_recommend
        <set>
            <if test="merchantId != null">
                merchant_id = #{merchantId},
            </if>
            <if test="recommendMerchantId != null">
                recommend_merchant_id = #{recommendMerchantId},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 查询（根据主键ID查询） -->
    <select id="selectById" resultType="com.jkm.hss.merchant.entity.Recommend">
        SELECT
        <include refid="Base_Column_List" />
        FROM tb_recommend
        WHERE id = #{id} and status=1
    </select>

    <!--查询所有记录-->
    <select id="selectAll" resultType="com.jkm.hss.merchant.entity.Recommend">
        SELECT
        <include refid="Base_Column_List" />
        FROM tb_recommend
        WHERE status = 1;
    </select>

    <!--我的好友-->
    <select id="myRecommend" resultType="com.jkm.hss.merchant.entity.RecommendShort">
        SELECT
        i.name,r.type,i.status
        FROM tb_recommend as r left JOIN tb_merchant_info as i on r.merchant_id=i.id
        WHERE r.recommend_merchant_id=#{merchantId} and r.status = 1;
    </select>

    <!--我的真实好友-->
    <select id="selectFriendCount" resultType="java.lang.Integer">
        SELECT
        count(1)
        FROM tb_recommend as r left JOIN tb_merchant_info as i on r.merchant_id=i.id
        WHERE r.recommend_merchant_id=#{merchantId} and r.status = 1 and i.status=6 and r.type=1;
    </select>

    <!--推荐我的人-->
    <select id="selectRecommend" resultType="com.jkm.hss.merchant.entity.Recommend">
        SELECT
        <include refid="Base_Column_List" />
        FROM tb_recommend
        WHERE merchant_id=#{merchantId} and status = 1;
    </select>

    <!--我推荐的好友-->
    <select id="selectRecommendByPage" resultType="com.jkm.hss.merchant.entity.RecommendShort">
        SELECT
        i.name,r.type,i.status
        FROM tb_recommend as r left JOIN tb_merchant_info as i on r.merchant_id=i.id
        WHERE r.recommend_merchant_id=#{merchantId} and r.status = 1 ORDER BY r.create_time DESC
    </select>

    <!--查询我的所有好友总条数-->
    <select id="selectRecommendCount" resultType="java.lang.Integer">
        SELECT
        count(1)
        FROM tb_recommend
        WHERE recommend_merchant_id=#{merchantId} and status = 1
    </select>

    <!--间接好友数量-->
    <select id="selectIndirectCount" resultType="java.lang.Integer">
        SELECT
        count(1)
        FROM tb_recommend
        WHERE recommend_merchant_id=#{merchantId} and type=2 and status = 1
    </select>

    <!--直接好友数量-->
    <select id="selectDirectCount" resultType="java.lang.Integer">
        SELECT
        count(1)
        FROM tb_recommend
        WHERE recommend_merchant_id=#{merchantId} and type=1 and status = 1
    </select>

    <!--推荐我的人-->
    <select id="selectDirectFriend" resultType="com.jkm.hss.merchant.entity.Recommend">
        SELECT
        <include refid="Base_Column_List" />
        FROM tb_recommend
        WHERE merchant_id=#{merchantId} and status = 1 and type=1;
    </select>
</mapper>
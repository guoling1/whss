<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.merchant.dao.UpgradeRecordDao">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id,	merchant_id,type,level,note,status,create_time,update_time
    </sql>

    <!-- 添加 （匹配有值的字段）-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_upgrade_record
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="merchantId != null">
                merchant_id,
            </if>
            <if test="type != null">
                type,
            </if>
            <if test="level != null">
                level,
            </if>
            <if test="note != null">
                note,
            </if>
            <if test="status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="merchantId!=null">
                #{merchantId},
            </if>
            <if test="type!=null">
                #{type},
            </if>
            <if test="level!=null">
                #{level},
            </if>
            <if test="note!=null">
                #{note},
            </if>
            <if test="status!=null">
                #{status},
            </if>
        </trim>
    </insert>

    <!-- 查询（根据主键ID查询） -->
    <select id="selectById" resultType="com.jkm.hss.merchant.entity.UpgradeRecord">
        SELECT
        <include refid="Base_Column_List" />
        FROM tb_upgrade_record
        WHERE id = #{id} and status=1
    </select>

    <!--查询所有记录-->
    <select id="selectAll" resultType="com.jkm.hss.merchant.entity.UpgradeRecord">
        SELECT
        <include refid="Base_Column_List" />
        FROM tb_upgrade_record
        WHERE status = 1;
    </select>

</mapper>
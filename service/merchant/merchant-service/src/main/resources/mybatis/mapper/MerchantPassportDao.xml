<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.merchant.dao.MerchantPassportDao">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_merchant_passport
        (
        merchant_id,
        token,
        type,
        expire_time,
        login_status,
        status
        )
        VALUES
        (
        #{merchantId},
        #{token},
        #{type},
        #{expireTime},
        #{loginStatus},
        #{status}
        );
    </insert>
    <update id="updateByMerchantId">
        UPDATE tb_merchant_passport
        SET
        merchant_id = #{merchantId},
        token = #{token},
        type = #{type},
        expire_time = #{expireTime},
        login_status = #{loginStatus},
        status = #{status}
        WHERE merchant_id = #{merchantId}
    </update>
    <update id="updateLoginStatusById">
        UPDATE tb_merchant_passport SET login_status = #{loginStatus} WHERE id = #{id}
    </update>
    <update id="updateExpireTimeById">
        UPDATE tb_merchant_passport SET expire_time = ${expireTime} WHERE id = #{id}
    </update>
    <update id="updateLoginStatusByMerchantId">
        UPDATE tb_merchant_passport SET login_status = #{loginStatus} WHERE merchant_id = #{merchantId}
    </update>
    <select id="selectByMerchantId" resultType="com.jkm.hss.merchant.entity.MerchantPassport">
        SELECT * FROM tb_merchant_passport WHERE merchant_id = #{merchantId}
    </select>
    <select id="selectByToken" resultType="com.jkm.hss.merchant.entity.MerchantPassport">
        SELECT * FROM tb_merchant_passport WHERE token = #{token} AND type = #{type}
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.account.dao.SettleAccountFlowDao">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_settle_account_flow
        (
        flow_no,
        settlement_record_id,
        account_id,
        settle_audit_record_id,
        account_user_type,
        order_no,
        before_amount,
        after_amount,
        out_amount,
        income_amount,
        app_id,
        trade_date,
        change_time,
        type,
        remark,
        status
        )
        VALUES
        (
        #{flowNo},
        #{settlementRecordId},
        #{accountId},
        #{settleAuditRecordId},
        #{accountUserType},
        #{orderNo},
        #{beforeAmount},
        #{afterAmount},
        #{outAmount},
        #{incomeAmount},
        #{appId},
        #{tradeDate},
        #{changeTime},
        #{type},
        #{remark},
        #{status}
        );
    </insert>

    <update id="updateSettlementRecordIdById">
        UPDATE tb_settle_account_flow
        SET settlement_record_id = #{settlementRecordId}
        WHERE id = #{id}
    </update>

    <update id="updateSettleAuditRecordIdByOrderNos">
        UPDATE tb_settle_account_flow
        SET settle_audit_record_id = #{settleAuditRecordId}
        WHERE order_no IN
        <foreach collection="orderNos" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

    <select id="selectById" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE id = #{id}
    </select>

    <select id="selectByOrderNoAndAccountIdAndType" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE order_no = #{orderNo} AND account_id = #{accountId} AND type = #{type}
    </select>

    <select id="statisticsYesterdayFlow" resultType="com.jkm.hss.account.helper.selectresponse.SettleAccountFlowStatistics">
        SELECT account_user_type, account_id, count(id) as count, SUM(income_amount) as amount
        FROM tb_settle_account_flow
        WHERE app_id = 'hsy'
        AND trade_date = #{tradeDate}
        AND type = 1
        GROUP BY account_user_type, account_id;
    </select>

    <select id="selectByAuditRecordId" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE settle_audit_record_id = #{recordId}
    </select>

    <select id="selectDealerOrCompanyFlowByOrderNo" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE order_no = #{orderNo} AND account_user_type != 2
    </select>

    <select id="selectBySettlementRecordId" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE settlement_record_id = #{settlementRecordId}
    </select>

    <select id="selectYesterdayDecreaseFlowCount" resultType="java.lang.Integer">
        SELECT count(0)
        FROM tb_settle_account_flow
        WHERE app_id = 'hsy'
        AND trade_date = #{tradeDate}
        AND type = 2
    </select>

</mapper>
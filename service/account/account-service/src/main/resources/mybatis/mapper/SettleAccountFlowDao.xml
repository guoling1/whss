<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.account.dao.SettleAccountFlowDao">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_settle_account_flow
        (
        account_id,
        account_user_type,
        order_no,
        before_amount,
        after_amount,
        out_amount,
        income_amount,
        app_id,
        trade_date,
        change_time,
        type,
        remark,
        status
        )
        VALUES
        (
        #{accountId},
        #{accountUserType},
        #{orderNo},
        #{beforeAmount},
        #{afterAmount},
        #{outAmount},
        #{incomeAmount},
        #{appId},
        #{tradeDate}
        #{changeTime},
        #{type},
        #{remark},
        #{status}
        );
    </insert>

    <select id="selectById" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE id = #{id}
    </select>

    <select id="selectByOrderNoAndAccountIdAndType" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow WHERE order_no = #{orderNo} AND account_id = #{accountId} AND type = #{type}
    </select>

    <select id="selectMerchantLastWordDayRecord" resultType="com.jkm.hss.account.entity.SettleAccountFlow">
        SELECT * FROM tb_settle_account_flow
        WHERE account_user_type = 2
        AND app_id != `hss`
        AND type = 1
        AND trade_date IN
        <foreach collection="tradeDates" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

</mapper>
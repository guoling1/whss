<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.dealer.dao.ShallProfitExceptionRecordDao">
    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        INSERT into tb_shall_profit_exception_record
        ( order_record_id, order_id, type, msg, status
        )VALUES(#{orderRecordId},#{orderId},#{type},#{msg},#{status})
    </insert>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.push.dao.PushDao">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_push_message_record
        (
        id,
        title,
        content,
        temp_type,
        push_type,
        client_id,
        targets,
        status
        )
        VALUES
        (
        #{pid},
        #{title},
        #{content},
        #{tempType},
        #{pushType},
        #{clientId},
        #{targets},
        #{status}
        )
    </insert>

    <update id="update">
        UPDATE tb_account
        SET
        user_name = #{userName},
        total_amount = #{totalAmount},
        available = #{available},
        frozen_amount = #{frozenAmount},
        due_settle_amount = #{dueSettleAmount},
        status = #{status}
        WHERE id = #{id}
    </update>




    <select id="selectUserAppBySid" resultType="Map">
        SELECT UID, SID,CLIENTID FROM tb_user_app WHERE SID  = #{sid}
    </select>


    <select id="selectUserAppByUid" resultType="Map">
        SELECT UID, SID,CLIENTID FROM tb_user_app WHERE UID  = #{uid}
    </select>


</mapper>
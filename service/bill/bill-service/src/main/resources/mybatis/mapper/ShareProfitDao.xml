<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.bill.dao.ShareProfitDao">


    <select id="selectAccountList" resultType="com.jkm.hss.bill.entity.JkmProfitResponse">
        SELECT * FROM tb_account WHERE id in (1,2,11,113,114)
        limit #{offset}, #{pageSize}
    </select>

    <select id="selectAccountListCount"  resultType="java.lang.Integer">
        SELECT COUNT(1) FROM tb_account WHERE id in (1,2,11,48,49)
    </select>


    <select id="selectAccountDetailsCount"  resultType="java.lang.Integer">
        SELECT COUNT(a.id) FROM tb_settle_account_flow a
        LEFT JOIN tb_account b ON a.account_id=b.id
        WHERE b.id=#{id}
    </select>

    <!-- 金开门公司账户明细 -->
    <select id="selectAccountDetails"  resultType="com.jkm.hss.bill.entity.JkmProfitDetailsResponse">
        SELECT a.* FROM tb_settle_account_flow a
        LEFT JOIN tb_account b ON a.account_id=b.id
        WHERE b.id=#{id}
        <if test="startTime!=null and startTime!=''">
            AND a.create_time <![CDATA[ >= ]]> #{startTime}
        </if>
        <if test="endTime!=null and endTime!=''">
            AND a.create_time <![CDATA[ <= ]]> #{endTime}
        </if>
        ORDER BY create_time DESC
        limit #{offset}, #{pageSize}
    </select>

</mapper>
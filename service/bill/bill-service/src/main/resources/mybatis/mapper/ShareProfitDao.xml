<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkm.hss.bill.dao.ShareProfitDao">


    <select id="selectAccountList" resultType="com.jkm.hss.bill.entity.JkmProfitResponse">
        SELECT * FROM tb_account WHERE id in (1,2,113,114)
        limit #{offset}, #{pageSize}
    </select>

    <select id="selectAccountListCount"  resultType="java.lang.Integer">
        SELECT COUNT(1) FROM tb_account WHERE id in (1,2,113,114)
    </select>

    <!-- 分润明细 -->
    <select id="selectProfitDetails"  resultType="com.jkm.hss.bill.entity.JkmProfitDetailsResponse">
        SELECT a.* FROM tb_split_account_record a
        LEFT JOIN tb_account b ON a.receipt_money_account_id=b.id
        WHERE b.id=#{id}
    </select>

</mapper>
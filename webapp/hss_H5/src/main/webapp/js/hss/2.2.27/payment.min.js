'use strict';

/**
 * Created by administrator on 2016/12/8.
 */

// 引入浏览器特性处理
var browser = _require('browser');
browser.elastic_touch();
// 引入http message
var message = _require('message');
var Keyboard = _require('keyboard');
new Keyboard({
  spanId: 'key-span',
  inputId: 'key-input',
  keyboardId: 'keyboard',
  isSelf: pageData.isSelf,
  selfId: 'layerSelf',
  notSelfId: 'layerNotSelf',
  amount: '2000'
});

function is_weixin() {
  var ua = navigator.userAgent.toLowerCase();
  return ua.match(/MicroMessenger/i) == "micromessenger";
}

if (pageData.isSelf == 1) {
  (function () {
    var layerSelf = document.getElementById('layerSelf');
    var cancelSelf = document.getElementById('cancelSelf');
    var submitSelf = document.getElementById('submitSelf');
    cancelSelf.addEventListener('click', function () {
      layerSelf.style.display = 'none';
    });
    submitSelf.addEventListener('click', function () {
      window.location.href = '/sqb/collection?oemNo='+pageData.oemNo;
    });
  })();
}
var layerNotSelf = document.getElementById('layerNotSelf');
var cancelNotSelf = document.getElementById('cancelNotSelf');
var submitNotSelf = document.getElementById('submitNotSelf');
cancelNotSelf.addEventListener('click', function () {
  layerNotSelf.style.display = 'none';
});
submitNotSelf.addEventListener('click', function () {
  if (is_weixin()) {
    WeixinJSBridge.call('closeWindow');
  } else {
    AlipayJSBridge.call('closeWebview');
  }
});
//# sourceMappingURL=payment.min.js.map

'use strict';

/**
 * Created by administrator on 2016/12/8.
 */

// 引入浏览器特性处理
var http = _require('http');
var message = _require('message');
var validate = _require('validate');
var browser = _require('browser');

var submit = document.getElementById('submit');
var branch = document.getElementById('branch');
var branchStorage = JSON.parse(localStorage.getItem('branch'));
submit.addEventListener('click', function () {
    if (branch.value == '') {
        message.prompt_show('请填写支行名称');
    } else {
        branchStorage.branchCode = "";
        branchStorage.branchName = branch.value;
        localStorage.setItem('branch', JSON.stringify(branchStorage));
        window.location.href = '/sqb/collection?oemNo=' + pageData.oemNo;
    }
});

// 处理 初始化 的过程
var dataStorage = JSON.parse(localStorage.getItem('branch'));
//# sourceMappingURL=addBranch.min.js.map

'use strict';

/**
 * Created by wangl on 2016/12/12.
 */

// 引入浏览器特性处理
var browser = _require('browser');
browser.elastic_touch('dealerIndex');
// 引入浏览器特性处理
var tools = _require('tools');
var aSpan = document.getElementsByClassName("decimal");
for (var i = 0; i < aSpan.length; i++) {
  aSpan[i].innerHTML = tools.NumToFix(aSpan[i].innerHTML);
}
//# sourceMappingURL=dealerIndex.min.js.map
